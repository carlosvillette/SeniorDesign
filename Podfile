# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'A.I.M.S.' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for A.I.M.S.
	pod 'Firebase/Core'
	pod 'Firebase/Auth'

  target 'A.I.M.S.Tests' do
    inherit! :search_paths
    # Pods for testing
  end

end
